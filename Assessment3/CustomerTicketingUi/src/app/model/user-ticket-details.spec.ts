import { UserTicketDetails } from './user-ticket-details';

describe('UserTicketDetails', () => {
  it('should create an instance', () => {
    expect(new UserTicketDetails()).toBeTruthy();
  });
});
