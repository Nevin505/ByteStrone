import { FilteropenPipe } from './filteropen.pipe';

describe('FilteropenPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteropenPipe();
    expect(pipe).toBeTruthy();
  });
});
